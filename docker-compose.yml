version: '3.8'

services:
  neon:
    build: .
    ports:
      - "8060:8060"
    volumes:
      - ./avatars:/app/avatars
      - ./application.properties:/app/application.properties  # 挂载外部配置文件
    environment:
      - SPRING_PROFILES_ACTIVE=prod